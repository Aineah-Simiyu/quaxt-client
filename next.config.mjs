/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ['localhost', 'api.quaxt.co.ke', 'api.qrserver.com'],          // allow QR server
	},
};

export default nextConfig;
